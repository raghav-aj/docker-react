sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t raghavn/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true raghavn/docker-react npm run test
